package Command;

import Bot.GroupInfo;
import org.jetbrains.annotations.NotNull;

public class CommandFactory {

    @NotNull
    public static Command Create(GroupInfo group, CommandData data) {
        return null;
    }
}
